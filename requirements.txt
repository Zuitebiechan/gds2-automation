# Core RPA
pywinauto>=0.6.8
pyautogui>=0.9.0

# Image processing (for screenshot comparison and fallback locators)
Pillow>=9.0.0
opencv-python>=4.5.0

# OCR (for text verification)
easyocr>=1.6.0

# VLM providers (for AI-based element finding)
anthropic>=0.40.0
openai>=1.0.0

# API (future)
fastapi>=0.100.0
uvicorn>=0.22.0

# Utilities
pydantic>=2.0.0

# Development
pytest>=7.0.0
pytest-cov>=4.0.0

# AI diagnosis (Phase 3.5)
zhipuai>=2.1.5

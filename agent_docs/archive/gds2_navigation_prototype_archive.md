# GDS2 Navigation Prototype Archive

This document preserves the old prototype notes under a neutral archive name.

## Status

- The previous graph-based navigation prototype has been removed.
- The current navigation flow is deterministic and HITL-friendly.
- The active runtime now lives in `diagnostic_platform/runtime/navigation_runtime.py`.

## Why This File Exists

- Preserve a stable archive name without the old prototype-era naming.
- Make it clear these notes are historical, not the current implementation source of truth.

## Current Source of Truth

- `README.md`
- `diagnostic_platform/runtime/navigation_runtime.py`
- `src/gds2_orchestration/`
# Archive Notice

This file is historical reference material. It does not define the current platform architecture. Start with `agent_docs/README.md` for the current documentation set.
